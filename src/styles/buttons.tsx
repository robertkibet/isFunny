import { StyleSheet } from "react-native";
import { colors } from "./global/colors-enums";

export const buttonStyles = StyleSheet.create({
  landing: {
    backgroundColor:colors.buttons.primary,
    color:colors.buttons.text.white
  },
});